export enum ThreadChannel {
  member = "member", // Cửa hàng
  customer = "customer", // Khách hàng
}

export enum ThreadStatus {
  new = "new", // Mở mới
  opening = "opening", // Đang mở tương tác
  closed = "closed", // Đã kết thúc
}
